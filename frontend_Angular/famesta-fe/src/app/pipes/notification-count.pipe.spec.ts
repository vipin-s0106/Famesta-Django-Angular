import { NotificationCountPipe } from './notification-count.pipe';

describe('NotificationCountPipe', () => {
  it('create an instance', () => {
    const pipe = new NotificationCountPipe();
    expect(pipe).toBeTruthy();
  });
});
